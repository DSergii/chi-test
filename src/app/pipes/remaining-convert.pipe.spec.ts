import { RemainingConvertPipe } from './remaining-convert.pipe';

describe('RemainingConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
